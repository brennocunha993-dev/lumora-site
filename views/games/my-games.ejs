<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meus Jogos - GamerSite</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --neon-cyan: #00d4ff;
            --orange: #ff6b35;
            --dark-bg: #0a0a0a;
        }
        
        body {
            background: linear-gradient(135deg, #000000 0%, #0a0a0a 50%, #000000 100%);
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
        }
        
        .navbar {
            background: rgba(0, 0, 0, 0.9) !important;
            backdrop-filter: blur(20px);
        }
        
        .navbar-brand {
            color: var(--neon-cyan) !important;
            font-weight: bold;
            font-size: 1.5rem;
        }
        
        .container {
            margin-top: 2rem;
        }
        
        .page-title {
            color: var(--neon-cyan);
            text-align: center;
            margin-bottom: 2rem;
            font-size: 2.5rem;
            text-shadow: 0 0 20px var(--neon-cyan);
        }
        
        .game-card {
            background: linear-gradient(145deg, rgba(0, 0, 0, 0.9), rgba(17, 17, 17, 0.8));
            border: 2px solid rgba(0, 212, 255, 0.3);
            border-radius: 15px;
            transition: all 0.3s ease;
            overflow: hidden;
            margin-bottom: 2rem;
        }
        
        .game-card:hover {
            transform: translateY(-5px);
            border-color: var(--neon-cyan);
            box-shadow: 0 10px 30px rgba(0, 212, 255, 0.3);
        }
        
        .game-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }
        
        .game-title {
            color: var(--neon-cyan);
            font-size: 1.3rem;
            font-weight: bold;
            margin-bottom: 0.5rem;
        }
        
        .game-status {
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: bold;
            text-transform: uppercase;
        }
        
        .status-approved {
            background: rgba(40, 167, 69, 0.2);
            color: #28a745;
            border: 1px solid #28a745;
        }
        
        .status-pending {
            background: rgba(255, 193, 7, 0.2);
            color: #ffc107;
            border: 1px solid #ffc107;
        }
        
        .status-rejected {
            background: rgba(220, 53, 69, 0.2);
            color: #dc3545;
            border: 1px solid #dc3545;
        }
        
        .btn-primary {
            background: linear-gradient(45deg, var(--neon-cyan), rgba(0, 212, 255, 0.8));
            border: none;
            color: #000;
            font-weight: bold;
        }
        
        .btn-primary:hover {
            background: linear-gradient(45deg, rgba(0, 212, 255, 0.8), var(--neon-cyan));
            transform: translateY(-2px);
        }
        
        .no-games {
            text-align: center;
            padding: 4rem 2rem;
            color: rgba(255, 255, 255, 0.6);
        }
        
        .no-games i {
            font-size: 4rem;
            color: var(--neon-cyan);
            margin-bottom: 1rem;
            opacity: 0.5;
        }
        
        .stats-card {
            background: linear-gradient(145deg, rgba(0, 0, 0, 0.9), rgba(17, 17, 17, 0.8));
            border: 2px solid rgba(0, 212, 255, 0.3);
            border-radius: 15px;
            padding: 1.5rem;
            text-align: center;
            margin-bottom: 2rem;
        }
        
        .stats-number {
            font-size: 2rem;
            font-weight: bold;
            color: var(--neon-cyan);
            text-shadow: 0 0 10px var(--neon-cyan);
        }
        
        .stats-label {
            color: rgba(255, 255, 255, 0.8);
            font-size: 0.9rem;
            text-transform: uppercase;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-gamepad me-2"></i>GamerSite
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link text-light" href="/games">
                    <i class="fas fa-arrow-left me-1"></i>Voltar
                </a>
            </div>
        </div>
    </nav>

    <div class="container">
        <h1 class="page-title">
            <i class="fas fa-folder me-3"></i>Meus Jogos
        </h1>

        <!-- Estatísticas -->
        <div class="row mb-4">
            <div class="col-md-3">
                <div class="stats-card">
                    <div class="stats-number"><%= games.length %></div>
                    <div class="stats-label">Total de Jogos</div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stats-card">
                    <div class="stats-number"><%= games.filter(g => g.approved === true).length %></div>
                    <div class="stats-label">Aprovados</div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stats-card">
                    <div class="stats-number"><%= games.filter(g => g.approved === false).length %></div>
                    <div class="stats-label">Pendentes</div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stats-card">
                    <div class="stats-number"><%= games.reduce((sum, g) => sum + (g.downloads || 0), 0) %></div>
                    <div class="stats-label">Downloads</div>
                </div>
            </div>
        </div>

        <!-- Lista de Jogos -->
        <% if (games.length === 0) { %>
            <div class="no-games">
                <i class="fas fa-gamepad"></i>
                <h3>Nenhum jogo encontrado</h3>
                <p>Você ainda não submeteu nenhum jogo.</p>
                <a href="/games/submit/new" class="btn btn-primary mt-3">
                    <i class="fas fa-plus me-2"></i>Submeter Primeiro Jogo
                </a>
            </div>
        <% } else { %>
            <div class="row">
                <% games.forEach(game => { %>
                    <div class="col-lg-4 col-md-6">
                        <div class="game-card">
                            <% if (game.images && game.images.length > 0) { %>
                                <img src="<%= game.images[0] %>" alt="<%= game.title %>" class="game-image">
                            <% } else { %>
                                <div class="game-image d-flex align-items-center justify-content-center" style="background: rgba(0, 212, 255, 0.1);">
                                    <i class="fas fa-gamepad" style="font-size: 3rem; color: var(--neon-cyan); opacity: 0.5;"></i>
                                </div>
                            <% } %>
                            
                            <div class="card-body p-3">
                                <div class="d-flex justify-content-between align-items-start mb-2">
                                    <h5 class="game-title"><%= game.title %></h5>
                                    <span class="game-status <%= game.approved === true ? 'status-approved' : (game.approved === false ? 'status-pending' : 'status-rejected') %>">
                                        <%= game.approved === true ? 'Aprovado' : (game.approved === false ? 'Pendente' : 'Rejeitado') %>
                                    </span>
                                </div>
                                
                                <p class="text-muted mb-2">
                                    <i class="fas fa-tag me-1"></i><%= game.genre || 'Sem categoria' %>
                                </p>
                                
                                <p class="text-light mb-3" style="font-size: 0.9rem; line-height: 1.4;">
                                    <%= game.description ? (game.description.length > 100 ? game.description.substring(0, 100) + '...' : game.description) : 'Sem descrição' %>
                                </p>
                                
                                <div class="d-flex justify-content-between align-items-center">
                                    <small class="text-muted">
                                        <i class="fas fa-calendar me-1"></i>
                                        <%= new Date(game.createdAt).toLocaleDateString('pt-BR') %>
                                    </small>
                                    
                                    <div class="d-flex gap-2">
                                        <% if (game.approved === true) { %>
                                            <a href="/games/<%= game._id %>" class="btn btn-sm btn-outline-info">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                        <% } %>
                                        
                                        <small class="text-muted d-flex align-items-center">
                                            <i class="fas fa-download me-1"></i>
                                            <%= game.downloads || 0 %>
                                        </small>
                                        
                                        <small class="text-muted d-flex align-items-center">
                                            <i class="fas fa-eye me-1"></i>
                                            <%= game.views || 0 %>
                                        </small>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>
        
        <div class="text-center mt-4">
            <a href="/games/submit/new" class="btn btn-primary btn-lg">
                <i class="fas fa-plus me-2"></i>Submeter Novo Jogo
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>